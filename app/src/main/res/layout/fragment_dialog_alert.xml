<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/alertParentView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_alert_dialog"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:id="@+id/alertTitleText"
        style="@style/AlertDialogTitleStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Remove all?" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/alertPositiveButton"
            style="@style/AlertDialogActionButton"
            android:layout_marginEnd="4dp"
            android:background="@drawable/bg_alert_positive_button"
            tools:text="Yes" />

        <TextView
            android:id="@+id/alertNegativeButton"
            style="@style/AlertDialogActionButton"
            android:layout_marginStart="4dp"
            android:background="@drawable/bg_alert_negative_button"
            tools:text="No" />

    </LinearLayout>


</LinearLayout>